import React from "react";

const Subheading = ({ text }) => {
    return <>
        <p style={{ textAlign: "center", color: "black", fontSize: "2rem", fontWeight: "bold", margin: "25px", marginTop: "100px" }}>{text}</p>
    </>;
};

export default Subheading;

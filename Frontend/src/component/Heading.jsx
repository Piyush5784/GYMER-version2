import React from "react";

const Heading = ({ text }) => {
    return <p style={{ fontFamily: "Judson", textAlign: "center", color: "black", fontSize: "2.5rem", fontWeight: "bold", margin: "25px" }}>{text}</p>
        ;
};

export default Heading;
